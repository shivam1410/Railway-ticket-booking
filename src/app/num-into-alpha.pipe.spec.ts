import { NumIntoAlphaPipe } from './num-into-alpha.pipe';

describe('NumIntoAlphaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumIntoAlphaPipe();
    expect(pipe).toBeTruthy();
  });
});
